-r requirements.txt

# Optional tooth-marking ML inference dependencies (CPU-only)
# Keeps production install lean and avoids CUDA/GPU wheels.
#
# Keep the versions paired according to the official PyTorch compatibility
# matrix. Validate both imports whenever either pin changes.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
torchvision==0.28.0+cpu
